{{yield}}


{{!-- {{#each @chapters as |chapter|}}
<H5pListItem @chapter={{chapter}} />
{{/each}} --}}

<div class="card">
        {{ember-chart
        type=@type
        data=@data
        width=700
        height=200
        options=@options
        }}
</div>