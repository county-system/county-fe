{{yield}}


{{!-- {{#each @chapters as |chapter|}}
<H5pListItem @chapter={{chapter}} />
{{/each}} --}}
{{log this.label}}

<div class="card">
        {{ember-chart
        type="bar"
        data=this.data
        width=900
        height=200
        options=this.options
        }}
</div>