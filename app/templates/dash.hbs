{{page-title "Dash"}}
{{outlet}}
<div>
    <h4 class="mb-4 text-muted">System Navigation </h4>
    <div class="row">
        <div class="col-md-3 col-lg-2 mb-4 align-middle">
            <span>{{ svg-jar "projects" width="110"}}</span>
            <h4 class="mt-4 text-muted">PROJECTS</h4>
            <LinkTo @route="projects">
                <a href="#" class="bg-green btn btn-lg">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4 align-middle">
            <span>{{ svg-jar "kpis" width="100"}}</span>
            <h4 class="mt-2 text-muted">KPI'S</h4>
            <LinkTo @route="kpi">
                <a href="#" class="bg-green btn btn-lg">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "county_health" width="110"}}</span>
            <h4 class="text-muted">COUNTY SYSTEMS HEALTH CHECK</h4>
            <LinkTo @route="systems">
                <a href="#" class="bg-green btn btn-lg">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "mon_and_eval" width="120"}}</span>
            <h4 class="mt-5 text-muted">COUNTY M&#38;E UPDATE</h4>
            <LinkTo @route="kpi" @disabled={{true}}>
                <a href="#" class="bg-green btn btn-lg">view</a>
            </LinkTo>
        </div>
    </div>
</div>