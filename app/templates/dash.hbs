{{page-title "Dash"}}
{{outlet}}
<div>
    <h4 class="mb-4 text-muted">System Navigation </h4>
    <div class="row">
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "projects" width="130"}}</span>
            <h4 class="">PROJECTS</h4>
            <LinkTo @route="projects">
                <a href="#" class="btn btn-primary btn-sm">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "kpi" width="130"}}</span>
            <h4 class="">KPI'S</h4>
            <LinkTo @route="kpi">
                <a href="#" class="btn btn-primary btn-sm">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "county_health" width="130"}}</span>
            <h4 class="">COUNTY SYSTEMS HEALTH CHECK</h4>
            <LinkTo @route="systems">
                <a href="#" class="btn btn-primary btn-sm">view</a>
            </LinkTo>
        </div>
        <div class="col-md-3 col-lg-2 mb-4">
            <span>{{ svg-jar "mon_and_eval" width="130"}}</span>
            <h4 class="">COUNTY M&38;E UPDATE</h4>
            <LinkTo @route="kpi">
                <a href="#" class="btn btn-primary btn-sm">view</a>
            </LinkTo>
        </div>
    </div>
</div>