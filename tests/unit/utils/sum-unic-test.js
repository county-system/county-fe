import sumUnic from 'county-fe/utils/sum-unic';
import { module, test } from 'qunit';

module('Unit | Utility | sumUnic', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = sumUnic();
    assert.ok(result);
  });
});
